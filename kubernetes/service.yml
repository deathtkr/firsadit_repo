apiVersion: v1
kind: Service
metadata:
  name: adi-test
  labels:
    app: adi-test
spec:
  type: LoadBalancer
  loadBalancerIP: 48.217.193.70  # Specify the desired public IP address
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: adi-test
